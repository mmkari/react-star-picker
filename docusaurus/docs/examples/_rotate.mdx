import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import {RotatePicker} from '@site/src/components/HomepageFeatures';

<RotatePicker />

<br/>

<details>
<summary>Show code</summary>
<p>

<Tabs>
<TabItem value="jsx" label="JSX">

```typescript
const RotatePicker = () => {
  const [rating, setRating] = React.useState<number | null>(null);

  return (
    <StarPicker //
      className="rotateStar"
      value={rating}
      onChange={setRating}
    />
  );
};
```
</TabItem>
<TabItem value="css" label="CSS">

```css
/* definitions for a "rotateStar" class */
.StarPicker.rotateStar .StarPickerButton {
  transition: transform 0.2s ease-in-out, color 0.4s;
}
.StarPicker.rotateStar .StarPickerButton.selected {
  transform: rotate(-60deg);
}
```

</TabItem>
</Tabs>

</p>
</details>
